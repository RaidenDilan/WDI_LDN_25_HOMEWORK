<%= form_for(country) do |f| %>
  <% if country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">

    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= f.label :image %>
      <%= f.text_field :image, class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= f.label :size %>
      <%= f.number_field :size, class: 'form-control', placeholder: 'km²'%>
    </div>

    <div class="form-group">
      <%= f.label :population %>
      <%= f.number_field :population, class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= f.label :language %>
      <%= f.text_field :language, class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= f.label :currency %>
      <%= f.text_field :currency, class: 'form-control'%>
    </div>

    <div class="form-group">
      <%= f.label :continent_id %>
      <%= f.collection_select :continent_id, Continent.all, :id, :name, class: 'form-control'%>
    </div>

    <div class="actions">
      <%= f.submit class: 'btn btn-primary submit'%>
    </div>

  </div>
<% end %>
